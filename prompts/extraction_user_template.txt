You will be provided with OCR-parsed text of a materials science paper.

=== INPUT TEXT START ===
{paper_text}
=== INPUT TEXT END ===

Execute the extraction workflow below and follow the system rules exactly.

Follow the laboratory extraction semantics as the source of truth.
- `Paper_Metadata` and `items` define the target semantic structure.
- The runtime `compositions` output is only the runtime-compatible encoding of those laboratory semantics.

Step 1: Global metadata and role distinction

- Extract the paper title and DOI into the root `Paper_Metadata` object.
- Extract this paper's own materials as `Target`.
- If explicit cited benchmark values are present, you may extract them as separate `Reference` items, but never mix them into a `Target` item.

Step 2: Item decomposition

- One item = one unique combination of composition + process.
- Split whenever composition, process state, sample/state label, route, orientation/loading mode, or parameter level changes.
- Give each item a stable `Sample_ID`; all tests that belong to the same specimen must share that id.
- If the paper defines an abbreviated sample nomenclature table and then reports direction/state-specific rows (for example `3090` in the process table and `B3090` / `S3090` in the property table), you must output one item per explicit row/state.
- For gradient materials, prefer one item with `Gradient_Material = true` unless the paper explicitly reports layer-specific independent data.
- Only split a gradient specimen into multiple items when independent per-layer or per-step data is explicit. In that case, keep `Gradient_Material = true` and link all split items with the same `Gradient_Group_ID`.
- If the same item has multiple testing conditions, keep one item and place all tests in that item's `Properties_Info`.
- If a variant is explicitly named but only sparse evidence exists, still keep a sparse item.

Step 3: Composition extraction (`Composition_Info`)

- Fill `Role`, `Alloy_Name_Raw`, `Nominal_Composition`, and `Measured_Composition`.
- Keep nominal and measured composition strictly separate.
- Record special composition annotations such as ranges, upper limits, or impurity notes in `Composition_Info.Note` / runtime `composition_note`.
- `nominal_composition` and `measured_composition` must be flat element->number maps only; never use step keys like `{"0": {...}, "5": {...}}`. If the paper truly reports explicit layer-specific graded data, create separate composition entries instead.
- Normalize reported composition to 100.0, using `other` only when justified by residual balance.
- Preserve mature commercial alloy names with null normalized arrays if no explicit numeric composition is reported.
- Never convert trade names or polymer abbreviations into fake element formulas.

Step 4: Process extraction (`Process_Info`)

- Build `Process_Text.original` and `Process_Text.simplified`.
- Extract explicit equipment information into `Process_Info.Equipment` / runtime `equipment`.
- Extract quantified variables into `Key_Params`.
- Use canonical parameter names where possible, and dynamic keys with unit hints when needed.
- Preserve heat-treatment ranges as strings such as `1273-1373` or `2-4`; never average them.
- For multistage processing, use stage suffixes such as `_Stage1_K` and never average stages.

Step 5: Microstructure extraction (`Microstructure_Info`)

- Build `Microstructure_Text.original` and `Microstructure_Text.simplified`.
- `Main_Phase` must describe the matrix phase. Do not label `Laves`, carbides, or gamma-prime as the main phase.
- Put explicit precipitate types into `Precipitates[]` / runtime `precipitates`.
- Extract `Main_Phase`, `Porosity_pct`, `Relative_Density_pct`, `Grain_Size_avg_um`, `Precipitate_Size_avg_nm`, and `Precipitate_Volume_Fraction_pct` whenever evidence exists.
- `Relative_Density_pct` must stay null when not reported. Never infer 100%.
- Put advanced quantified microstructure metrics under `Advanced_Quantitative_Features`.

Step 6: Properties extraction (`Properties_Info`)

- Extract all reported properties for the same item.
- Keep strict ordering from Tier 1 to Tier 3.
- Use `Elongation_Total`, `Elongation_Uniform`, and `Elongation_At_Fracture` instead of the older generic elongation field.
- If the paper reports only a generic `Elongation`, map it to `Elongation_Total` and note that explicitly.
- Use compressive property names only for compressive contexts.
- `Test_Temperature_K` is only the actual testing environment temperature, not a process temperature.
- Also extract `Strain_Rate_s1`, `Tensile_Speed_mm_min`, `Hardness_Load`, `Hardness_Dwell_Time_s`, `Data_Source`, `Test_Specimen`, and `Note` when supported.
- For ranges, keep midpoint in `Value_Numeric` and source range in `Value_Range`.
- For mean±std, keep mean in `Value_Numeric` and std in `Value_StdDev`.

- For hardness/microhardness, if unit text is omitted but context indicates Vickers hardness, set `Unit` to `HV`.

Step 7: Table alignment and generalization checks

- Reconstruct by header-lock, row-lock, temperature-lock, and unit-lock.
- Do not guess from ambiguous OCR or unlabeled plot lines.
- Build the full list of explicit labels and parameter levels before finalizing output.
- Ensure every clearly supported variant has a corresponding item.
- Do not overfit to one phrase pattern; preserve generalized extraction behavior across different paper layouts.

Step 8: Mandatory final self-audit

- Check Target/Reference purity.
- Check no explicit variant, test temperature, or property column is silently dropped.
- Check MPa/GPa values are not assigned to percentage properties, and percentage values are not assigned to strength fields.
- Check the final payload is valid JSON only.

Important output rule:

- Follow the laboratory schema semantics rooted at `Paper_Metadata` and `items`, but emit the runtime internal schema rooted at `compositions`.
- Use field mapping consistent with the system prompt so downstream pipeline stages remain compatible.
- Do not output any prose or markdown.

Example F (Label-driven split is mandatory)
INPUT (abstracted):
- Same chemistry, labels listed as: AB, CR, PDA-873, PDA-973.
DECISION:
- Output 4 separate items (one per label), not one merged item.
WRONG:
- One item with sample id "AB,CR,PDA-873,PDA-973".

Example G (Parameter-level split is mandatory)
INPUT (abstracted):
- Same chemistry with VED levels: 50, 65, 83 J/mm3.
DECISION:
- Output 3 separate items, one per VED level.
WRONG:
- One item with `Volumetric_Energy_Density_J_mm3: "50-83"` while rows are level-specific.

Example H (Comparison-route retention)
INPUT (abstracted):
- Paper reports SLM alloy and cast baseline, both with explicit property rows.
DECISION:
- Keep both as separate items (Role by provenance), do not drop cast/baseline rows.

Example I (Intermediate state must be kept)
INPUT (abstracted):
- Route chain: As-built -> Cold Rolled (CR) -> PDA-873 -> PDA-973.
- CR has clear process text but no complete property table.
DECISION:
- Output a standalone CR item with available process/microstructure fields.
WRONG:
- Skip CR and connect As-built directly to PDA states.

Example J (Do not mix parameter cohorts)
INPUT (abstracted):
- Same alloy has two SLM cohorts: optimized (200 W, 800 mm/s) and another parameter set.
- Strength table rows are reported separately for each cohort.
DECISION:
- Keep two separate items (one per cohort), each internally consistent.
WRONG:
- Using process params from cohort A but mechanical properties from cohort B in one item.

Example K (Do not drop level/chemistry variants)
INPUT (abstracted):
- SLM family reports VED levels 50/65/83 with separate evidence snippets.
- Comparison section reports cast variants with explicit formulas (e.g., ...W2, ...Mo2) and numeric properties.
DECISION:
- Output separate items for each VED level and each cast chemistry variant.
WRONG:
- Keep only one SLM item plus one generic cast item.

Example L (Do not mix AB and AA properties)
INPUT (abstracted):
- Same alloy reports two states: as-built (AB) and as-annealed (AA).
- Tensile/fatigue table has separate AB and AA rows.
DECISION:
- Create AB item and AA item; each keeps only its own row values.
WRONG:
- AB item containing both AB and AA strength/fatigue entries.

Example M (Keep named variants even without numeric rows)
INPUT (abstracted):
- Caption/body explicitly names variants: VED 50, 65, 83 and cast W2/Mo2 counterparts.
- Numeric values are not available as legal table/text cells for all variants.
DECISION:
- Keep separate variant items (can be sparse) for each named variant.
- Do not invent plot-pixel values.
WRONG:
- Collapse all variants into one generic alloy item because only one averaged numeric set is available.

Example N (Gradient specimen should stay whole unless layer data is explicit)
INPUT (abstracted):
- Process says: 42 layers pure A, then 12 layers each at 5/10/15/20 wt% B.
DECISION:
- Keep one gradient item with `Gradient_Material = true` if the paper only describes the graded schedule overall.
WRONG:
- Splitting into 5 step items without explicit layer-specific independent data.

Example O (Split gradient specimen only when independent layer data is explicit)
INPUT (abstracted):
- Paper gives separate layer-specific compositions or properties for 0/5/10/15/20 wt% B.
DECISION:
- Output one item per explicit layer/step, keep `Gradient_Material = true`, and link them with the same `Gradient_Group_ID`.
