# KnowMat GPU dependencies (Windows/Linux with NVIDIA GPU only)
# Installation order:
#   1. pip install -r requirements.txt
#   2. pip uninstall -y paddlepaddle paddlepaddle-gpu
#   3. pip install -r requirements-gpu.txt -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
#   4. conda install nvidia::cudnn cuda-version=12
#
# For detailed instructions, see docs/ocr-cudnn64_9-fix.md and docs/platforms.md

# GPU version of PaddlePaddle runtime
paddlepaddle-gpu==3.3.0
