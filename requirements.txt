# KnowMat 依赖（与 environment.yml 中 pip 部分一致）
# 仅用 pip 安装时：pip install -r requirements.txt
#
# Paddle 默认使用 GPU（cu129），建议单独执行以指定国内源：
#   python -m pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
#   python -m pip install "paddleocr[all]"
# cuDNN 9（GPU OCR 必需，否则报 cudnn64_9.dll error 126）：conda install nvidia::cudnn cuda-version=12

# 项目本体（可编辑安装）
-e .

# LangChain & 编排
langchain==0.3.26
langchain-community==0.3.27
langchain-openai==0.3.27
langgraph==0.6.10
trustcall==0.0.39

# OpenAI / API
openai==1.93.2

# PaddleOCR-VL（PDF 解析，默认 GPU cu129）
paddlepaddle-gpu==3.3.0
paddleocr[all]
pymupdf==1.24.13

# OCR 相关传递依赖
beautifulsoup4>=4.12
lxml>=6.0
cssselect>=1.4
einops>=0.8
openpyxl>=3.1
premailer>=3.10

# 校验与配置
pydantic==2.11.7
pydantic-settings>=2.0.0
python-dotenv==1.1.1
colorlog==6.9.0
PyYAML>=6.0

# 向量与嵌入
sentence-transformers==5.0.0
torch==2.8.0
torchvision==0.23.0
